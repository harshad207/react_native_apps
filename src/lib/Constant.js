/* eslint-disable prettier/prettier */
export const STORAGE = {
  TOKEN: 'token',
  PROFILE: 'profile',
};

export const AUTH_URL = 'authaccount';
export const BASE_URL = {
  LOGIN_URL: `${AUTH_URL}/login`,
  REGISTER: `${AUTH_URL}/register`,
};
